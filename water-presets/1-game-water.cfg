# Preset 1: the default. The pool is drawn with the game's own lake water material, on the game's water mesh
# layout, so it looks like still lake water of the same depth. Nothing to tune; "log" writes the pool's real
# mesh, material and shader-global values to Player.log.
log
