# Preset 3: the game's untouched fountain water, exactly as the exported model references it
# (no tint, no UV fix, no motion changes). A baseline for both the look and any lag.
legacy
native
uv1 off
log
